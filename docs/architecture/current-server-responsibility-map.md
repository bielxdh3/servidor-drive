# Root.ark Current Server Responsibility Map

## 1. Purpose and scope

This document is the code-backed Phase 4.0 responsibility map for Issue #5. It records the current server composition, ownership, coupling, persistence and storage paths, security/data-safety invariants, validation evidence, and a bounded extraction order.

Branch reconciliation addendum (2026-08-13): This map intentionally preserves its historical analysis baseline of repository `bielxdh3/root.ark`, branch `codex/folders-acl`, and SHA `18f44f3fc59b9660f24f161a67a214a4e44980e9`. Live repository metadata now verifies the canonical/default branch as `Root/main` at SHA `28747c6ebdac873650e2d5a3c6193824e7cc9985` (`git ls-remote --symref origin HEAD refs/heads/Root/main`; `gh repo view bielxdh3/root.ark --json defaultBranchRef`). This records branch provenance only; it does not rewrite the historical responsibility analysis or claim remote Issue #14 closure.
Extraction follow-up addendum (2026-08-13): In the local, uncommitted working tree based on canonical SHA `28747c6ebdac873650e2d5a3c6193824e7cc9985`, the bounded realtime transport was moved to `src/realtime/server.js`; `server.js` retains thin `broadcastRealtime`/`broadcastDataChanged` adapters and preserves the shared `parseBoundedNumber` helper used by WebDAV configuration. Local validation passed twice for `test/realtime-transport-boundaries.test.js` (7/7 each), `test/auth-security.test.js` (13/13), `test/realtime-webdav-meta-remediation.test.js` (62/62), and the combined realtime/auth run (20/20), with syntax and whitespace checks passing. This is local evidence only; buffered-client close, browser, CI, provider, production, and remote-closure evidence remain unverified.
Upload scanning follow-up addendum (2026-08-13): The current compatibility scanning/quarantine boundary now lives in the deliverable CommonJS module `src/upload-scanning.js`; `server.js` retains a thin `scanUploadBeforePending` adapter for simple, chunked, and WebDAV PUT entry points. The module preserves extension blocking, ClamAV INSTREAM parsing, quarantine containment/metadata/audit, and explicit scanner fail-open/fail-closed behavior. Local disposable `test/upload-security.test.js` passed 16/16 twice, alongside the realtime/auth/meta-remediation suites; live ClamAV and D-009 client-side scanning remain unverified/unimplemented.
Cloud inventory follow-up addendum (2026-08-13): Cloud coordination was already isolated in `services/cloudStorage.js`, so no redundant runtime extraction was made. Mock-only inventory contracts now cover S3 prefix/key containment, Google Drive parent and metadata binding, and duplicate provider identities; `test/cloud-storage.test.js` passed 27/27 twice. External S3/Google Drive credentials, provider runtime, and D-003/D-009 zero-knowledge compatibility remain unverified.
WebDAV validation follow-up addendum (2026-08-13): Existing direct-protocol WebDAV boundaries remain inline because the route, metadata journal, cloud intent, and D-009 local-bridge requirements make a safe extraction larger than this packet. Disposable local suites for executable/oversize/abort paths, completed-journal recovery, crash consistency, and the meta-remediation matrix passed 90/90 combined. This does not prove an OS mount, external provider, production deployment, or final zero-knowledge WebDAV bridge.
Persistence follow-up addendum (2026-08-13): After rebuilding the native `better-sqlite3` binding in the disposable worktree, focused SQLite recovery, migration-generation, trash-persistence, and archive/staging suites passed 70/70. This is targeted safety evidence only; full JSON/SQLite semantic parity, long-running migration, and the production-mandatory SQLite decision remain open.
Continuation architecture addendum (2026-08-13): The accepted Phase C/D documents are `docs/validation/2026-08-13-json-sqlite-parity-matrix.md`, `docs/architecture/rootark-bielos-relationship-contract.md`, and `docs/architecture/zero-knowledge-migration-contract.md`. They provide source-backed parity planning, a locally closure-ready independent Root.ark/BielOS boundary, and a reviewable zero-knowledge architecture/migration contract. They do not change runtime behavior, prove full parity or migration, make Root.ark zero-knowledge today, approve future owner decisions, close remote Issue #10, or establish production/browser/provider/OS-mount behavior.


Analysis baseline: repository `bielxdh3/root.ark`, default branch `codex/folders-acl`, exact default SHA `18f44f3fc59b9660f24f161a67a214a4e44980e9` (`docs: record product discovery round 3 (#45)`).

Current-system warning: this maps the implementation that exists at the baseline SHA. It is not the final D-001 through D-009 architecture, and existing server-readable behavior must not be mistaken for product approval.

The inspection was bounded to `server.js`, its directly registered/imported modules, relevant repositories/services/middleware/routes, directly relevant tests, and the plan, security, validation, and product-discovery documents. No runtime code, tests, dependencies, issues, or Git history were changed.

## 2. Server composition summary

`server.js` is the integration spine. It is 7,900 lines and currently owns startup, configuration, filesystem layout, persistence adapters, authorization helpers, cloud adapters, realtime setup, most route families, lifecycle coordination, and background workers (`server.js:1-43`, `server.js:45-165`, `server.js:7884-7900`).

Existing partial boundaries are present for:

- authentication routes and login throttling: `src/routes/auth.js`;
- analytics, audit, backup, and trash route registration: `src/routes/{analytics,audit,backups,trash}.js`;
- HTTP and realtime authentication: `src/middlewares/auth.js`;
- permission middleware: `src/middlewares/permissions.js`;
- repositories for users, folders, uploads, sharing, permissions, expirations, versions, encryption metadata, analytics, audit, history, backups, and trash;
- backup, restore, trash, cloud-storage, preview, and backup-scheduler services.

The following remain primarily inline in `server.js`: users/folders/file-access routes, uploads and scanning, previews and encryption routes, file versions, public links, WebDAV, file rename/move, and the realtime server itself. Route registration passes large callback contexts into the partial modules (`server.js:4492-4504`, `server.js:7278-7333`), so those modules reduce route size without removing the central dependency graph.

Startup creates the HTTP/WebSocket servers, validates `JWT_SECRET`, configures JSON/static middleware, creates local runtime directories, runs SQLite migrations when enabled, seeds users/folders, schedules backups, repairs/cleans temporary data, starts cloud-trash/restore retry work, and listens on the configured port (`server.js:45-165`, `server.js:4322-4438`, `server.js:7884-7900`).

## 3. Current responsibility map

Evidence references use repository-relative `file:line` locations. “Ready” describes readiness for a future bounded extraction, not product completeness.

| Domain | Current owner and boundary | Persistence/storage and coupling | Security/data-safety invariants | Tests/evidence and current limitation | Readiness |
|---|---|---|---|---|---|
| Startup and configuration | `server.js:45-165`, `server.js:4322-4400`; creates Express/HTTP/WebSocket, validates secrets, configures limits, initializes runtime paths and middleware. | Reads environment; creates `data`, `uploads`, `temp`, quarantine, chunk and incoming directories; calls migrations and seed/load helpers. | Missing/weak `JWT_SECRET` fails startup; runtime paths must stay inside the runtime root; initialization must not expose secrets or overwrite user data. | `test/startup-acceptance.test.js`; engineering baseline and operational report. Startup is a central dependency hub. | Low; keep as integration root until smaller boundaries are proven. |
| Authentication and session transport | `src/routes/auth.js`; `src/middlewares/auth.js`; `/auth/login`, `/auth/logout`, `/auth/me`, `/auth/session.js`; WebSocket transport in `src/realtime/server.js`, constructed and broadcast through thin adapters in `server.js`. | User records through `loadUsers`/`saveUsers`; JWT contains username and `sessionVersion`; browser uses `rootark_session` HttpOnly/Lax cookie plus readable CSRF cookie. | Current server-side user and permissions are authoritative; disabled/deleted users and mismatched generations fail; cookie writes require CSRF and origin evidence; WebSocket requires cookie-only auth, expected Origin, expiry and freshness. | `test/auth-core-boundaries.test.js`, `test/auth-security.test.js`, `test/realtime-transport-boundaries.test.js`; Phase 2.2 closure audit and browser-session threat model. Login JSON still contains an unused token. | Realtime transport extraction accepted locally; auth remains a shared compatibility boundary. |
| Authorization and permissions | `createRequirePermission` in `src/middlewares/permissions.js`; `server.js:1853-1935`, `server.js:2893-3044`, route-level `hasFolderAccess`, `hasFileAccess`, `hasFileEditAccess`, and `canManage*` helpers. | Permission metadata is stored with users/folders/files in JSON or SQLite; many inline routes perform a folder lookup followed by a second file/owner check. | Authorization is server-side and must be evaluated against the current user; folder and file access must fail closed; path and folder IDs must resolve to the requested scope. | Auth/security tests cover session freshness and representative permission removal; operational report covers denial paths for upload/trash/share. Full route-by-route matrix is not complete. | Medium; centralization should follow one domain extraction and preserve each existing decision. |
| Users and account administration | Inline `/users`, `/users/:username`, and `/auth/*` composition, `server.js:4492-4616`; user repository. | `data/users.local.json`/seed JSON or SQLite `users`; JSON mode keeps a Git-ignored per-username generation ledger; SQLite keeps soft-deleted generation. | Passwords are bcrypt hashes; users cannot delete themselves; password/role/permission/disabled changes increment `sessionVersion`; deletion removes permissions and prevents stale identity recreation. | `test/auth-security.test.js`, `test/users-repository.test.js`, Phase 2.2 audit. User-management route remains tightly coupled to auth, audit, and persistence. | Low; outside Phase 4.0 extraction target. |
| Folders and folder permissions | Inline `/folders`, `/folders/:id/access`, `/folders/:id/name`, `/folders/:id/temporary`, `/folders/:id`; `server.js:4824-5068`; folders repository. | Folder records in JSON/SQLite; per-folder directories under `uploads` and `temp`; deletion cascades metadata and trash paths. | Folder access is checked before listing or mutation; IDs and names are normalized; folder deletion must preserve trash/metadata and cloud cleanup rules. | `test/trash-security.test.js`, `test/trash-route-persistence.test.js`, operational trash and SQLite evidence. Folder-specific parity and failure coverage remain partial. | Low to medium; shared dependency of nearly every file domain. |
| Files, listing, downloads, search, and open tokens | Inline `/list`, `/files/search`, `/files/:name`, `/file-open-token`, `/open-file/:token/:name`; `server.js:5095-5214`, `server.js:6988-7028`. | Local file bytes under folder upload directories; cloud cache is hydrated through `ensureCloudFileCached`; short-lived in-memory open-file tokens. | `path.basename`, folder access, file access, expiration/trash checks, and permission checks precede reads; open tokens are short-lived and scoped. | Operational report validates list/share/download portions; no complete search/open-token matrix. | Low; compatibility surface is broad. |
| Upload and chunked upload | Inline middleware and routes `/upload` and `/upload-chunk`; `server.js:4699-4819`, `server.js:5411-5654`; `multer`, gzip handling, chunk metadata. | Incoming files in `temp/.incoming`; chunks in `temp/.chunks`; pending metadata in JSON/SQLite; approved bytes move into folder upload storage and may sync to cloud. | Upload permission and folder access precede Multer; filenames are bounded/canonicalized; chunks have bounded count/session IDs; compressed input is decompressed before scan/approval; pending state is separate from published state. | `test/upload-security.test.js`; operational U-01–U-04 pass. Chunked-upload and live scanner coverage are explicitly incomplete; U-05 ClamAV detection is blocked. | Medium-low; three upload entry paths and scan/quarantine/lifecycle coupling make this riskier than realtime. |
| Upload scanning and quarantine | `src/upload-scanning.js` owns scanning/quarantine logic; `server.js` retains the thin `scanUploadBeforePending` adapter and `/quarantine` routes. | `data/quarantine.json` plus `data/quarantine` files; not represented by a repository or SQLite table; quarantine deletion removes both metadata and payload. | Suspicious extensions and scanner results block publication or quarantine; quarantine filenames are sanitized and contained; `UPLOAD_FAIL_CLOSED` controls unavailable-scanner behavior and must remain explicit in any extraction. | `test/upload-security.test.js`; operational U-03/U-04 pass and U-05 is environmental `BLOCKED`; CI does not prove live ClamAV. | Bounded extraction accepted locally; chunked/WebDAV/provider parity and the future D-009 scanning model remain open. |
| Preview and document parsing | `services/documentPreviewService.js` plus inline access/response routes `/preview/file/:scope/:name` and `/preview/text/:scope/:name`; `server.js:3736-3760`, `server.js:7579-7604`. | Reads local/cloud-cached file bytes; parser worker uses bounded input/output, DOCX preflight, queue and child-process isolation. | Preview access reuses folder/file authorization; unsupported/oversized/parser-failure cases fail closed without exposing content; derived server-readable previews conflict with the future trust model. | `test/document-preview-service.test.js`, executable/meta-remediation tests; engineering baseline notes browser and full hostile-corpus limits. | Medium for service-only work, low for route extraction until trust-model direction is resolved. |
| Encryption metadata and modes | Inline encryption metadata/key helpers and routes `/encrypted/:filename/metadata`, `/encrypted-download/:filename`, `/encrypted/:filename/grant-access`, `/encrypted/:filename/revoke-access`; `server.js:194-200`, `server.js:2462-2600`, `server.js:5214-5365`. | `data/encrypted-files.json` or `encrypted_files` table; server master-key file and per-file metadata; file bytes remain in local/cloud storage. | Access to encrypted metadata/download is permission- and key-aware; no secret/token/password fields belong in audit logs; metadata/file moves must remain coordinated. | Upload and backup tests cover representative metadata persistence; no complete encryption-mode or key-rotation matrix. Current `none`, `server-key`, `user-key`, `password`, and `dual` modes are not D-007’s final client-side design. | Low; high security and future-product conflict. |
| File versions and lifecycle | Inline `recordApprovedFileVersion`, `syncFileVersionsToCloud`, rename/move helpers, and `/versions`, version download/open-token/restore/delete; `server.js:2026-2278`, `server.js:6988-7276`, `server.js:7755-7882`. | Version history in JSON/SQLite; version bytes share the folder upload directory with generated stored names; cloud copies are synchronized asynchronously. | Approval creates or replaces a current version; current versions cannot be deleted; restore creates a new current version; rename/move must update links, permissions, expirations, versions, encrypted metadata, audit/history, and cloud copies. | Operational report proves representative upload/approval/backup/trash flows; no complete direct version-route matrix. | Low; high-risk coordination with storage, encryption, sharing, trash, and audit. |
| Trash and permanent deletion | `services/trashService.js`, `repositories/trashRepository.js`, `src/routes/trash.js`, plus inline `/delete/:name` and folder deletion; `server.js:4206-4320`, `server.js:5053-5068`, `server.js:7705-7753`. | Trash metadata is JSON or `trash_items`; payloads under `data/trash`; cloud deletion is queued with durable remote state and retry claims. | Move-to-trash is distinct from permanent deletion; restore checks owner/manager and folder access; safe child paths and metadata restoration are required; remote failures remain visible and retryable. | `test/trash-security.test.js`, `test/trash-local-completion.test.js`, `test/trash-remote-state.test.js`, route-persistence tests; operational T-01/T-02 pass. Cloud-provider runtime remains unproven. | Low; data-loss impact and many cross-domain callbacks. |
| Public links and sharing | Inline `/share`, `/share/:token`, password/view/download/preview/QR/file routes and `resolveShareAccess`; `server.js:1773-1836`, `server.js:7335-7550`. | `data/public-links.json` or `public_links`; link token, expiry, view/download counters and password/session state. File bytes are local/cloud cached. | Public-share routes are intentionally outside browser auth; token validation, expiry, password session, view/download limits, trash blocking, and generic failure responses are required; link secrets must not enter logs. | `test/auth-security.test.js` and operational C-06/share evidence cover representative public-link behavior. Complete recipient/revocation matrix remains open. | Low; separate trust boundary and D-009 server-blind-sharing conflict. |
| Audit, analytics, and action history | Audit/analytics/history persistence adapters in `server.js:602-825`, `server.js:828-1105`; extracted read routes in `src/routes/analytics.js` and `src/routes/audit.js`; inline event calls throughout routes. | Analytics, audit, history JSON or SQLite tables; audit archives to `data/audit-logs-archive.json`; current code retains roughly one year of analytics/audit data while D-008 specifies shorter defaults. | Audit values strip credential-like keys and bound strings; actor/IP/user-agent are recorded without content, keys, tokens, or passwords; security-relevant mutations should emit audit and, where applicable, analytics/history/realtime events. | `test/auth-security.test.js`, backup/trash/upload tests, operational C-01–C-06; no complete audit event contract. Many callers construct event payloads manually. | Medium for a later shared-event boundary; not safe to extract incidentally with a route family. |
| Backup, restore, retention, and scheduling | `services/backupService.js`, `services/restoreService.js`, `services/backupScheduler.js`, `repositories/backupRepository.js`, `src/routes/backups.js`; startup wiring `server.js:7884-7889`. | Backup archives under runtime data, history in JSON/SQLite, lock/takeover/retention journals, optional cloud restore-sync metadata; restore uses temporary extraction and SQLite journals. | Auth/backup permission and explicit restore confirmation are required; archive paths are allowlisted; locks, manifests, rollback journals, fsync, integrity checks, and cleanup protect against data loss. | Backup/restore/resilience/meta-remediation tests; operational B-01–B-04 pass; cloud backup and long-running retention are outside local proof. | Low; one of the highest coordination/data-loss surfaces. |
| JSON and SQLite persistence | `server.js:167-177`, adapter functions `server.js:290-635`, repositories, `db/index.js`, `db/migrations.js`, and `db/migrate-json-to-sqlite.js`. | JSON files remain available for fallback/legacy writes; SQLite uses configured `DATABASE_URL` or `data/rootark.sqlite`, WAL, foreign keys, and migrations. Tables cover users, folders, pending uploads, public links, history, permissions, expirations, versions, encrypted metadata, analytics, audit, backups, trash, and session fields. | Fallback/write flags must not silently change behavior; migration and restore must preserve rows and rollback capability; runtime roots and database paths must remain isolated. | `test/users-repository.test.js`, SQLite migration/recovery/backup/trash tests; operational C-01–C-06 pass. Behavioral parity is not complete and remains Phase 4.2 work. | Low; do not simplify modes during Phase 4.0. |
| Local, S3, and Google Drive storage | `services/cloudStorage.js` provides provider-neutral `enabled/status/key/inventory/upload/download/remove/removePrefix/list`; `server.js:202-261` provides adapters and call sites. Local files are the default. | Local: `uploads/{folder}` and `temp/{folder}`. Cloud keys are `prefix/{uploads|temp}/{folder}/{file}`; S3 uses object keys, Google Drive uses app properties and configured parent folder. | Provider keys reject traversal/foreign prefixes; downloads clean partial files; Drive inventory validates parent and metadata identity; asynchronous cloud errors must not be silently treated as local success when lifecycle state depends on them. | `test/cloud-storage.test.js`, backup/trash/WebDAV cloud tests; operational report does not claim external S3/Drive runtime proof. | Low; broad call graph and multiple storage modes. |
| WebSocket realtime authentication and notifications | `src/realtime/server.js` owns the WebSocket lifecycle and transport boundary; `server.js` retains thin `broadcastRealtime`/`broadcastDataChanged` adapters, and route mutations call those adapters. | In-memory client set and per-socket rate/activity state; no persistence. Event shape is `{event,payload,timestamp}` and `data:changed` carries a source plus payload. | Cookie-only session, expected Origin, current user/session version, expiry refresh before messages/sends, bounded payload/buffer/rate limits, binary rejection, heartbeat/idle termination, and no events before authentication. | `test/auth-security.test.js`, `test/auth-core-boundaries.test.js`, `test/realtime-transport-boundaries.test.js`, Phase 2.2 closure audit and successful Security Regression evidence. Buffered-client closure remains unverified. | Bounded extraction accepted locally; buffered-client closure remains unverified. |
| WebDAV | Inline Basic Auth, path parsing, file/folder resolution, handlers, journaling, cloud reconciliation, and registration `server.js:955-1035`, `server.js:5740-6920`; mount path defaults to `/dav`. | Uses local/cloud folder bytes and the WebDAV move journal under runtime temp; updates links, permissions, expirations, versions, encryption metadata, audit/history, and cloud state. | Separate Basic Auth boundary; path traversal, depth/body/method limits, folder/file permissions, overwrite and rollback/journal states, and safe audit paths are required. | `test/realtime-transport-boundaries.test.js` and four WebDAV remediation/recovery suites; operational W-01 passes and W-02 OS mount is blocked. | Low; large surface, high data-loss coordination, separate auth, and D-009 local-bridge conflict. |
| Synchronization | `sync-client/rootark-sync.js`; uses bearer `/auth/login`/`/auth/me`, `/list`, `/pending`, `/approve/:name`, `/upload`, and version state. | Client-side `.rootark-sync.json` and `.rootark-sync-state.json` live outside the repository in normal use; server uses pending uploads, approved files, versions, and local/cloud storage. | Client path containment and ignored sensitive extensions; bearer token is deliberately separate from browser cookie/CSRF flow; upload limits and approval state must be preserved. | Operational S-01–S-04 pass, including restart, changed-file propagation, renewal, old-token observation, and size limit; bidirectional sync is not implemented and D-009 requires a later dedicated design. | Low; current one-way MVP is not the final synchronization boundary. |
| Static/browser delivery | `express.static("./public")`, cache headers, `/auth/session.js`, and browser bootstrap; `server.js:4372-4399`. | Reads tracked public assets; no server persistence. | No browser storage bearer tokens or WebSocket URL tokens; authenticated bootstrap is no-store and escapes identity data; state-changing browser requests use same-origin credentials and CSRF. | `test/auth-security.test.js` source checks plus recorded dashboard/browser validation; login response token remains a residual risk. | Low; outside the server extraction scope. |
| Shared path, validation, error, and response helpers | Inline helpers `getFolderStoragePath`, `isSafeChildPath`, `getAccessibleFolderOrRespond`, `shortenFileName`, `isExistingFile`, `sanitizeAuditValue`, plus per-route status/error JSON; `server.js:889-1105`, `server.js:3217-3541`, `server.js:3475-3491`. | Determines local paths, cloud keys, JSON/SQLite keys, audit payloads, and response shapes. | Canonicalize filenames/segments, reject traversal/absolute/unsafe paths, preserve generic auth errors, keep response shapes stable, and avoid logging secrets/content. | Scattered focused tests cover selected path/auth/upload/WebDAV/backup cases; no single helper contract. | Medium as a proven shared boundary; centralize only after callers and compatibility behavior are measured. |

## 4. Persistence and storage matrix

| State | JSON path(s) | SQLite table/repository | File/object payload | Notes |
|---|---|---|---|---|
| Users and session generations | `data/users.local.json`, `data/user-generations.local.json` | `users` / `usersRepository` | Password hashes only in user records | JSON generation ledger is metadata-only and Git-ignored; SQLite preserves soft-deleted generation. |
| Folders and access metadata | `folders.json`, `file-permissions.json`, `file-expirations.json` | `folders`, `file_permissions`, `file_expirations` | Folder directories under `uploads` and `temp` | Server adapters may read SQLite, fall back to JSON, and optionally write both. |
| Pending uploads and encryption metadata | `pending-uploads.json`, `encrypted-files.json` | `pending_uploads`, `encrypted_files` | Pending bytes in temp; quarantine is separate | Approval coordinates pending metadata, file bytes, versions, cloud, audit, analytics, and history. |
| Sharing and activity | `public-links.json`, `analytics.json`, `audit-logs.json`, `actions-history.json`, audit archive | `public_links`, `analytics_events`, `audit_logs`, `actions_history` | None beyond metadata | Callers emit events directly; payload contracts are not centrally typed. |
| Versions | `file-versions.json` | `file_versions` | Current and stored version files under folder upload directories; optional cloud copies | Rename/move/trash/restore must update both metadata and bytes. |
| Trash | `trash-items.json` | `trash_items` | `data/trash` payloads and optional remote objects | Durable remote deletion states are part of the lifecycle, not an incidental cleanup. |
| Backup/restore | `backup-history.json` | `backup_history` | Backup archives, lock/takeover/retention/restore journals | Archive and restore paths are runtime-root-owned and must remain disposable/isolated in tests. |
| Quarantine | `quarantine.json` only | None | `data/quarantine` | This is a JSON-plus-payload boundary with no SQLite parity. |
| Local and cloud file bytes | None | None | `uploads`, `temp`, S3 keys, or Google Drive objects | Cloud provider keys are normalized and scoped under the configured prefix and folder. |
| Database runtime | None | `data/rootark.sqlite` or resolved `DATABASE_URL` | SQLite WAL/SHM and migration/backup material | `db/index.js` enables WAL, foreign keys, and busy timeout; restore uses explicit journaling. |

## 5. Current implementation versus D-001 through D-009

| Decision | Current implementation | Boundary to preserve or reconcile |
|---|---|---|
| D-001: Root.ark/BielOS relationship | This repository is an independent Node/Express runtime with independent data, sessions, and authentication. | Do not share runtime state, identity, keys, or databases with BielOS during extraction. |
| D-002: private users and compartments | Admin-created users, folders, and permissions exist; public links are a separate anonymous principal. Full compartment/tenancy contract is not implemented. | Preserve current isolation checks and do not infer that current folders equal final compartments. |
| D-003: client-side zero-knowledge content | Current server supports server-readable encryption modes, server-side previews/scanning, and metadata-dependent flows. A reviewable zero-knowledge architecture/migration contract is now documented separately. | This remains an explicit product conflict; implementation, migration, and acceptance remain open, and no extraction may expand server plaintext access or call current behavior the final contract. |
| D-004: bounded admin powers and audit | Current role/permission helpers, audit actor capture, sanitization, and audit routes provide a partial implementation. | Preserve least-privilege checks and secret/content exclusions; detailed support access remains open. |
| D-005: accounts, recovery, deletion, retention | Current admin account CRUD, session revocation, trash, permanent deletion, and backup retention exist. Invitations, user recovery, and final deletion policy are not complete. | Keep revocation distinct from deletion and do not claim external-copy deletion. |
| D-006: trusted-device/recovery lifecycle | No final client key lifecycle, recovery package, or offline authorization boundary exists in the current server. | Treat current session and server-key behavior as provisional, not a target design. |
| D-007: versioned client cryptography | Current file versions and encryption metadata are server-coordinated; per-file client keys and audited recovery-package verification are not implemented. | Version extraction must not be presented as cryptographic architecture work. |
| D-008: retention and cryptographic erasure | Current code uses roughly one-year analytics/audit retention and backup/trash workflows; it does not prove backup-aware key invalidation or truthful cryptographic erasure. | Do not change retention or claim erasure during this map or a compatibility extraction. |
| D-009: zero-knowledge compatibility | Current scanning, preview, public links, native server WebDAV, and one-way sync are server-readable or server-mediated. A reviewable zero-knowledge architecture/migration contract now documents the target boundaries. | Implementation, migration, provider integration, local bridge, bidirectional sync, and acceptance remain unverified/open; current behavior is not the final contract. |

## 6. Concrete duplication and coupling inventory

| Observation and exact locations | Classification | Extraction implication |
|---|---|---|
| `createAuthenticate`/`createRealtimeAuthenticator` in `src/middlewares/auth.js` and `createRequirePermission` in `src/middlewares/permissions.js` are shared gates, while many routes additionally call `hasFolderAccess`, `hasFileAccess`, `hasFileEditAccess`, and `canManage*` in `server.js:1853-1935`, `server.js:2893-3044`. | `INTENTIONAL-SHARED-BOUNDARY` | Preserve middleware plus route-specific resource decisions; do not replace all checks with a single generic role check. |
| `path.basename` and string normalization are repeated in users/files/uploads/versions/sharing/WebDAV routes; stronger containment helpers are separately defined in `server.js:1010-1035`, `server.js:3217-3258`, `server.js:3475-3491`, and `services/cloudStorage.js:24-41`. | `DUPLICATED-CANDIDATE` | A future path-helper extraction needs a caller inventory and response-compatibility tests. |
| `shouldUseDatabase`, `shouldReadJsonFallback`, `shouldWriteLegacyJson`, and paired `load*`/`save*` adapters cover users, folders, pending uploads, links, permissions, expirations, versions, encryption, analytics, audit, and history (`server.js:167-177`, `server.js:290-635`, `server.js:904-930`). | `DUPLICATED-CANDIDATE` | This is not safe to simplify in Phase 4.0; JSON/SQLite parity and rollback remain Phase 4.2 work. |
| Cloud operations have a provider-neutral service, but `server.js` wraps and schedules them in `syncFolderCacheFromCloud`, `syncFileToCloud`, `deleteCloudFileLater`, version sync, trash deletion, restore sync, and WebDAV move recovery (`server.js:202-261`, `server.js:2151-2169`, `server.js:4256-4319`, `server.js:6615-6689`). | `HIGH-RISK-COORDINATION` | Cloud extraction must preserve local cache semantics, remote retries, version variants, and failure visibility. |
| `auditLog`, `logAnalyticsEvent`, and `addActionHistory` are shared writers, but each route constructs its own event type/details and often emits all three (`server.js:670-760`, `server.js:1063-1105`, approval/rename/move/trash routes). | `DUPLICATED-CANDIDATE` | Centralize event contracts only with migration-safe evidence; an extraction must preserve event names and redaction. |
| Extracted routes receive large callback contexts and inline routes build Portuguese error JSON independently (`server.js:4492-4504`, `server.js:7278-7333`, route families throughout `server.js`). | `DUPLICATED-CANDIDATE` | Response-shape compatibility is part of every future extraction; do not introduce a generic error framework now. |
| Approval, rename, move, version restore/delete, trash, and WebDAV MOVE coordinate bytes, metadata repositories, cloud objects, audit, analytics, history, and realtime (`server.js:2107-2278`, `server.js:6050-6092`, `server.js:6701-6824`, `server.js:7125-7267`, `server.js:7705-7882`). | `HIGH-RISK-COORDINATION` | These are lifecycle transactions spread across the integration spine; they are poor first extractions. |
| Encryption metadata is updated by upload approval, rename, move, sharing/download restrictions, and version paths (`server.js:2462-2600`, `server.js:5295-5365`, `server.js:7622-7635`, `server.js:7755-7865`). | `FUTURE-PRODUCT-CONFLICT` | Current server-readable modes cannot be converted into D-007/D-009 by a compatibility-only extraction. |
| Backup scheduling and cleanup start at boot, while several independent intervals process temporary files, trash, cloud trash, restore sync, and incoming uploads (`server.js:7884-7899`). | `HIGH-RISK-COORDINATION` | Worker ownership and shutdown behavior must be specified before moving background work. |
| Realtime freshness is checked in `src/realtime/server.js` at authentication, before each message, and before each send; `server.js` retains thin construction/broadcast adapters, while event producers remain distributed across persistence and lifecycle routes. | `INTENTIONAL-SHARED-BOUNDARY` | This distributed producer/central transport shape is the basis for the accepted transport-only extraction. |

## 7. Test and evidence matrix

| Candidate | Focused tests | Integration/operational/CI evidence | Missing coverage and readiness |
|---|---|---|---|
| Realtime authentication and notifications | `test/auth-security.test.js`, `test/auth-core-boundaries.test.js`, `test/realtime-transport-boundaries.test.js` | Phase 2.2 closure audit traces cookie/Origin/current-user/session-version/expiry; Security Regression runs cited by the audit passed; 594-test baseline passed at the recorded operational SHA. Local extraction and focused contract coverage are accepted; buffered-client closure remains unverified. | Preserve the extracted module/adapters and add a deterministic public buffered-client contract only if a truthful fixture becomes available. |
| Upload scanning/quarantine | `test/upload-security.test.js` and related real-server upload tests | Operational U-01–U-04 passed; U-03 proves suspicious extension quarantine; CI baseline covers extension blocking and selected upload boundaries. Local bounded extraction is accepted with preserved adapters and quarantine behavior. | Chunked/WebDAV/provider scan-entry parity, live ClamAV detection, and D-009 client-side/unverified scanning remain open. |
| Cloud storage | `test/cloud-storage.test.js`, backup/trash/WebDAV cloud suites | Provider-contract tests and CI exist; local operational run deliberately did not claim external S3/Google Drive behavior. | Need provider-backed disposable integration, cache/retry/lifecycle matrix, and backup/trash completeness; not first. |
| WebDAV | `test/realtime-transport-boundaries.test.js`, `test/realtime-webdav-*.test.js` | W-01 direct protocol matrix passed; W-02 OS mount is environmental `BLOCKED`; exact-head CI for the remediation work is recorded in issue/validation history. | Full mount, auth/metadata/cloud matrix and failure/overwrite evidence remain broader than one extraction; not first. |
| Versions and file lifecycle | `test/upload-security.test.js`, trash/backup suites, operational upload/trash/backup evidence | Approval, trash, backup, and representative version metadata persistence are exercised. | No focused complete version-route/rename/move/encryption/public-link matrix; high coupling; not ready. |
| Encryption | upload, backup, preview, and metadata-related tests | Representative metadata persistence is covered; product discovery explicitly leaves algorithms and key lifecycle open. | The architecture/migration contract is documented; exact primitives, implementation, migration, and acceptance remain open, and no extraction is authorized yet. |
| Remaining route families | Route-specific tests are distributed across auth, upload, trash, backup, and realtime suites. | Aggregate CI proves syntax/tests/audit, not complete route contracts. | No bounded candidate; scope is too broad for one coherent PR. |
| Persistence-mode simplification | repository, migration, SQLite recovery, backup, and trash tests | C-01–C-06 operational checks passed; JSON-to-SQLite migration was idempotent and restart reads were verified. | Full JSON/SQLite behavioral parity and rollback evidence are not complete; reserved for Phase 4.2. |

Tests prove exercised or intended behavior only. They do not establish complete correctness for every route, provider, deployment, or failure mode.

## 8. Extraction-readiness ranking

Ratings are qualitative and evidence-backed; they are not a score pretending to be precise.

| Rank | Candidate | Coverage | Security/data-loss impact | Coupling and modes | Compatibility/future pressure | Reversibility / one-PR fit | Decision |
|---:|---|---|---|---|---|---|---|
| 1 | Realtime authentication and notifications | High focused coverage | High security, low persisted-data mutation | Small-to-medium; one in-memory mode | Medium; preserve event envelope and D-001 independence | High / high | Locally accepted bounded extraction; buffered-client closure remains unverified |
| 2 | Upload scanning and quarantine | Medium | High security and publication integrity | Large; simple, chunked, and WebDAV uploads; JSON-plus-files | Medium-high; D-009 changes scanning trust model | Medium / medium-low | Locally accepted bounded extraction; defer broader parity work |
| 3 | Cloud storage | Medium unit/contract coverage | High data-loss and provider-failure impact | Large; local/S3/Google Drive and background retries | High; D-003/D-009 unresolved | Medium / low | Defer until external-provider evidence exists |
| 4 | WebDAV | Medium-high direct protocol coverage | High data-loss and separate-auth impact | Very large; files, metadata, cloud, journal, retry | High; future local bridge | Low / low | Defer |
| 5 | File versions and lifecycle | Partial | High data-integrity impact | Very large; files, metadata, cloud, encryption, trash | High | Low / low | Defer |
| 6 | Encryption | Partial metadata coverage | Critical security impact | Large and key-state dependent | Very high; D-007/D-009 architecture/migration contract is documented, but implementation and exact choices remain open | Low / low | Do not extract before product/security implementation decisions |
| 7 | Remaining route families | Distributed | Mixed | Broad and unclear | High | Low / low | Not a bounded candidate |
| 8 | Persistence-mode simplification | Strong migration evidence but incomplete parity | High data-loss impact | Cross-cutting JSON/SQLite state | Medium-high; rollback decision open | Low / low | Phase 4.2 only |

> Historical proposal boundary (2026-08-13): Sections 9-10 preserve the Phase 4.0 extraction proposal, invariants, acceptance criteria, rollback rationale, and non-goals at the pre-extraction baseline. Current local ownership and evidence are recorded in Sections 1-8 and the extraction follow-up addenda above; the historical proposal is not a statement that the extraction remains future work.

## 9. Recommended first extraction

### Candidate

Extract the current WebSocket transport and notification fan-out boundary: authentication at upgrade, client lifecycle, heartbeat/idle handling, message validation/rate limits, freshness checks, `sendRealtime`, `broadcastRealtime`, and `broadcastDataChanged`.

### Proposed target boundary

Create a small CommonJS realtime module, for example `src/realtime/server.js`, with injected dependencies for `WebSocket`, cookie parsing, expected-origin calculation, current-user authentication/freshness, runtime limits, and the HTTP server. It should return the transport lifecycle and broadcast functions. `server.js` should retain thin compatibility adapters so existing route producers continue emitting the same event names, payloads, and timestamp envelope.

### Expected files to move or change

- `server.js`: move only the WebSocket construction/lifecycle block (`server.js:52-65`, `server.js:263-287`, `server.js:4401-4438`) and pass the returned broadcaster to existing route contexts/callers.
- New `src/realtime/server.js`: own transport lifecycle, connection authentication, heartbeat, freshness, rate and payload bounds, and event fan-out.
- Tests: add the missing focused contract tests listed below before moving code. No tests are added by this Phase 4.0 map.

### Invariants to preserve

- Cookie-only browser WebSocket authentication; no bearer token in the URL.
- Expected-Origin validation before the `connected` event.
- HS256 claim validation, current user lookup, disabled/deleted rejection, `sessionVersion` comparison, and expiry enforcement before each authenticated message/send.
- Maximum payload and buffered bytes, disabled compression, binary-message rejection, per-window message limit, heartbeat, idle timeout, and close cleanup.
- Exact `{event,payload,timestamp}` envelope and `data:changed` source/payload shape.
- Existing producer calls and route response behavior; no event is emitted to unauthenticated or stale sockets.

### Explicit exclusions

Do not change JWT claims, cookie/CSRF semantics, route names, response shapes, event names, frontend code, WebDAV Basic Auth, upload scanning, cloud providers, persistence, encryption, backup/trash lifecycle, or product behavior. Do not introduce a new message protocol or reconnect policy.

### Prerequisite tests already present

- `test/auth-security.test.js`: permission removal, deletion/recreation, expiry, and real active-WebSocket revocation.
- `test/auth-core-boundaries.test.js`: claim and cookie parsing boundaries.
- `test/realtime-transport-boundaries.test.js`: payload/compression/binary/burst configuration and cookie/Origin/message boundaries.
- `docs/security/phase-2-2-closure-audit.md` and `docs/security/browser-session-threat-model.md`: source-backed transport and freshness invariants.

### Missing tests required before moving code

Add focused tests that fail if the extraction changes: rejected upgrade before `connected`, exact first-event ordering, stale/expired send rejection, heartbeat and idle termination, buffered-client close, message-rate reset, exact event envelope, and cleanup of the heartbeat timer on server close. Keep them in disposable fixtures and avoid real credentials or runtime data.

### Acceptance criteria for the future extraction PR

1. Only the transport boundary moves; the working-tree diff contains no unrelated route, persistence, provider, frontend, or product changes.
2. Existing realtime/auth tests and the new contract tests pass at the exact base and extracted head.
3. Browser WebSocket URLs remain credential-free and Origin/session freshness checks remain active.
4. Existing event names, payloads, response shapes, and producer call sites remain unchanged.
5. No new runtime artifacts, credentials, tokens, databases, uploads, or backups are created in the repository.
6. The PR documents the unchanged limitation that login JSON still contains an unused token.

### Rollback and reversibility

Keep the initial module as a thin adapter with no persistence migration. A single revert can restore the three inline blocks in `server.js`; no data, route, event, or provider state needs migration. Do not delete the old behavior until the extracted head has passed the focused and exact-head CI checks.

### Why safer than the next two alternatives

Realtime has the smallest state surface and the strongest direct security regression evidence, and it does not move file bytes or metadata. Upload scanning touches simple, chunked, and WebDAV entrypoints, quarantine files, pending state, and an unavailable live ClamAV dependency; cloud storage touches every file lifecycle plus two external provider modes and lacks external operational proof. Both alternatives therefore have larger coupling, more persistence/storage modes, and more direct D-009 conflict.

## 10. Explicit non-goals

- No runtime extraction or refactor in Phase 4.0.
- No new tests, dependencies, route names, response shapes, or issue bodies.
- No security inventory or re-run of Issue #11.
- No implementation of D-001 through D-009.
- No changes to `docs/security/current-findings.md`, `docs/product-discovery.md`, source code, tests, package files, workflows, branches, commits, issues, or PRs.
- No default-branch rename work for Issue #14.

## 11. Unresolved evidence gaps

- The configured workspace drive was unavailable during the original analysis. The map was prepared, validated, and published from a fresh checkout of the exact required base; no additional transplant remains pending.
- Live ClamAV detection and actual OS WebDAV mounting remain environmental blockers recorded by the operational report.
- S3/Google Drive runtime behavior, cloud backup/trash, and production proxy/HTTPS/Origin behavior are not proven by the local validation record.
- JSON/SQLite behavior is not fully equivalent; quarantine has no SQLite representation; final persistence convergence is Phase 4.2.
- Complete version, encryption, public-link, route-wide authorization, and lifecycle failure matrices are missing.
- Product decisions leave exact cryptographic algorithms, key envelopes, recovery, quarantine, sharing, synchronization, WebDAV bridge, and backup erasure mechanics open.
